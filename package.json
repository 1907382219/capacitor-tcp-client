{
  "name": "capacitor-tcp-client-android",
  "version": "0.0.5",
  "description": "一个用于 Android 平台的 Capacitor TCP 客户端插件。",
  "main": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf ./dist",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "GaliGG_CC",
  "license": "ISC",
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.5"
  },
  "files": [
    "android/src/main/",
    "android/build.gradle",
    "dist/",
    "CapacitorTcpClient.podspec"
  ],
  "keywords": [
    "capacitor",
    "plugin",
    "native"
  ],
  "capacitor": {
    "ios": {
      "src": "ios"
    },
    "android": {
      "src": "android"
    }
  }
}
