{
  "name": "capacitor-tcp-client",
  "version": "1.0.0",
  "description": "A Capacitor plugin for TCP client functionality",
  "main": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf ./dist",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.5"
  },
  "files": [
    "dist/",
    "ios/",
    "android/",
    "CapacitorTcpClient.podspec"
  ],
  "keywords": [
    "capacitor",
    "plugin",
    "native"
  ],
  "capacitor": {
    "ios": {
      "src": "ios"
    },
    "android": {
      "src": "android"
    }
  }
}
